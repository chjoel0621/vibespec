interface:
  display_name: "VibeSpec"
  short_description: "Create and semantically review product plans"
  brand_color: "#7957F2"
  default_prompt: "Use $vibespec to turn my product idea into a validated SOT JSON, semantic KPI review, and editable HTML viewer."

policy:
  allow_implicit_invocation: true
